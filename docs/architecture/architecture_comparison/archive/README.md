# architecture comparison archive

`docs/architecture/architecture_comparison/archive/` 保存 architecture comparison 框架的历史生成结果。

新的比较运行不要覆盖归档结果；默认应写入 `tmp/architecture_comparison_results/` 或命令行显式指定的输出目录。
