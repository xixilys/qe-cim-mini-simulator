# Generic DSE design spaces

Active design-space definitions for domain-neutral accelerator exploration live here.
Legacy application-specific Bayesian design-space files were moved under
`legacy/` for review before deletion.
